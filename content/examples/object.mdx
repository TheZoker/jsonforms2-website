---
id: object
title: Object
---

import { Object } from '../../src/components/examples/Object'

The object renderer is used, when an object with type `object` is used. 

To get a good overview, have a look at the `Schema` tab in the demo below.

<Object />
